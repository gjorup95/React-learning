# React session #8 - Building custom hooks
Applying the knowledge i have gained to create a custom hook that handles a request to a youtube API and creates a initial state to be shown in the app component.
